name: "dagacs_frontend"
description: "DAGACS M1 - Attendance Governance System"

environment:
  sdk: "^3.8.0"

dependencies:
  flutter:
    sdk: flutter
  http: ^0.13.6
  shared_preferences: ^2.0.17
  flutter_secure_storage: ^9.0.0

flutter:
  uses-material-design: true

dev_dependencies:
  flutter_test:
    sdk: flutter